# AI Session Logs - 2026-07-18

## Context
- **Project:** VietBridge---APT
- **System Architecture:** Hybrid 3-tier real-time meeting translator (Frontend Static Site + NestJS Backend Gateway + Python FastAPI STT/Engine).
- **Focus:** Audio capture audit, local environment debugging, and Render deployment preparation.

## Activity Summary
1. **Audio Pipeline Audit & Optimization:**
   - Investigated browser audio capture (`captureAdapter.ts`, `WebAudioStreamRepository.ts`) and verified `getUserMedia` constraints.
   - Confirmed browser noise suppression and echo cancellation are explicitly configured for clean mono PCM extraction (`pcm-processor.js` worklet @ 16kHz) optimized for VAD processing without clipping.
   - Diagnosed console logs (`[CaptureAdapter] Expected 1 channel, got 2`) and verified it as an informational notice during mono extraction rather than a critical failure.

2. **Backend Gateway Investigation (`vietbridge-backend`):**
   - Investigated frontend `BACKEND_UNAVAILABLE: Cannot reach the VietBridge backend. Check that port 3000 is running` error.
   - Built and verified local NestJS server starting on `0.0.0.0:3000`, confirming WebSocket gateway routing (`/api/rooms`, `/api/sessions`, `turn.start`, `audio.chunk`).

3. **Render Deployment Environment Standardization:**
   - Designed complete deployment templates (`.env.render`) for all three major components to eliminate manual environment variable configuration:
     - `stt/.env.render`: Python 3.11.9 setup, `STT_HOST=0.0.0.0`, `STT_PORT=10000`, FPT Cloud (`vi`) and Groq (`en`) API configuration.
     - `backend/.env.render`: Production Node environment, port binding, CORS origin configuration, and remote STT service integration (`STT_BASE_URL`).
     - `frontend/.env.render`: Build-time environment configuration (`VITE_BACKEND_API_URL`, `VITE_BACKEND_WS_URL`, `VITE_TRANSPORT=socketio`).

## Notes
- Prepared system for full production deployment onto Render Free Tier.
