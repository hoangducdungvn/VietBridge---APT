import type { TranscriptSegment } from '@domain/entities/TranscriptSegment';

// Use-case placeholder for future domain validation around translating a final segment.
export class TranslateSegment {
  execute(segment: TranscriptSegment) {
    return segment;
  }
}
