# API Contract Overview

The canonical input contract is owned by Voice and lives at:

- `../docs/audio-streaming-contract.md`

The gateway-to-client STT/MT output extension lives at:

- `docs/stt-mt-output-contract.md`

This file is only a routing note so schema details are not duplicated or silently forked.
